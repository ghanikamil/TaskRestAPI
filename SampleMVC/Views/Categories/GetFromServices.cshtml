@model IEnumerable<SampleMVC.Models.Category>

@{
    ViewData["Title"] = "GetFromServices";
}

<h1>GetFromServices</h1>

<p>
    <a asp-action="CreateFromServices">Create New</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.categoryID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.categoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.categoryID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.categoryName)
            </td>
            <td>
                @Html.ActionLink("Edit", "EditFromServices", new { id=item.categoryID}) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "DeleteFromServices", new { id = item.categoryID })
            </td>
        </tr>
}
    </tbody>
</table>
